import React from "react"
import RoomLayout from "../components/RoomLayout"

const STATUS = {
  LOADING: "loading",
  LOADED: "200",
  NOT_FOUND: "404",
}

export default () => {
  return <RoomLayout />
}
